```
const { listLoading, stableFetchComplete, forceRefresh } = useFetchTouenItems({
    nearestClientName,
    previewRowsOnce,
    customers,
    touenItems,
    setTouenItems,
    setItemObject,
    setProducts,
    setProductsMap,
  })
  ```
